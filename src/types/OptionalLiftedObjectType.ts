import type { LiftedObjectType } from './LiftedObjectType.js';

/** Optional lifted JS object — undefined when a quad carries no matching base triple. */
export type OptionalLiftedObjectType = LiftedObjectType | undefined;

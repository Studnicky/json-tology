import type { ReferenceTargetType } from '../types/ReferenceTargetType.js';

/** Map from property name to effective lift property (graph+node). */
export type EffectivePropertyMapType = Map<string, ReferenceTargetType>;

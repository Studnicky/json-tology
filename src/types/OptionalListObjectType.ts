import type { QuadObjectType } from './Quad.js';

/** Optional list quad object — undefined when the term type is not a valid quad object position. */
export type OptionalListObjectType = QuadObjectType | undefined;

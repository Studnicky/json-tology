/** @internal — CLI visualization option shape; consumed only by the viz subpath, not the public package surface. */
export type VizOptionsType = {
  'noOpen': boolean;
  'output': string;
  'schema': string;
};

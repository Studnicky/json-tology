/** @internal — CLI build option shape; not part of the public package surface. */
export type BuildOptionsType = {
  'baseIri'?: string;
  'format': string;
  'output': string;
  'outputFile'?: string;
  'schema': string;
};

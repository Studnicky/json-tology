import type { ReferenceTargetType } from '../types/ReferenceTargetType.js';

/** A type IRI resolved to its graph and node, or undefined when unknown. */
export type ResolvedTypeNodeType = ReferenceTargetType | undefined;

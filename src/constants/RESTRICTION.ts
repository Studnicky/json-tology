/**
 * Restriction — runtime constants for the OWL restriction phantom-tag protocol.
 */

/**
 * The phantom property key used to tag `RestrictionReferenceEntity` objects.
 * Produced only by `Compose` factory methods.
 */
export const RESTRICTION_TAG = '~jt:restriction';
